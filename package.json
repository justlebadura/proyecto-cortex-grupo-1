{
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "dev:full": "bash ./start.sh"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "react-latex-next": "^3.0.0"
  },
  "devDependencies": {
    "tailwindcss-animate": "^1.0.7"
  }
}
